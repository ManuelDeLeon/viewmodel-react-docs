StarterProject({
  render(){
    <div class="ui segment">
      <h2 class="ui header">Starter Project</h2>
      <a class="anchor active" id="BasicsStarterProject"></a>
      <p>
        You can use the <a target="_blank" href="https://github.com/ManuelDeLeon/viewmodel-react-starter">viewmodel-react-starter</a> project to play with ViewModel.
      </p>

      <p>Download the starter project:</p>
      <pre><code>git clone https://github.com/ManuelDeLeon/viewmodel-react-starter.git</code></pre>

      <p>Install dependencies:</p>
<pre><code>cd viewmodel-react-starter
npm install</code></pre>

      <p>Run tests:</p>
      <pre><code>npm test</code></pre>

      <p>Start the app:</p>
      <pre><code>npm start</code></pre>

      <h3>Meteor</h3>
      <p>If you're using Meteor then check out the <a target="_blank" href="https://github.com/ManuelDeLeon/viewmodel-react-starter-meteor">viewmodel-react-starter-meteor</a> project.</p>
    </div>
  }
});